HTML Code
<article class="col-md-4">
                    <p>An example of the calculation being performed <b>A=π(R2﹣r2)</b></p>
                    <div>
                        <input type="number" class="form-control" id="tbOuterRadius" placeholder="Enter Outer Radius" />
                        <input type="number" class="form-control" id="tbInnerRadius" placeholder="Enter Inner Radius" />
                    </div>
                    <div>
                        <input type="button" class="btn btn-warning" value="Calculate Ring Area" onclick="ringAreaCalculator()" />
                    </div>
                    <span id="ringAreaOutput"></span>
                </article>

JavaScript Code
function ringAreaCalculator() {
    let innerRadius = document.getElementById('tbInnerRadius').value;
    let outerRadius = document.getElementById('tbOuterRadius').value;
    let inner = 3.14 * innerRadius * innerRadius;
    let outer = 3.14 * outerRadius * outerRadius;
    let circleRadiusOutput = inner - outer;
    let calcRadiusOutput = document.getElementById('ringAreaOutput');
    calcRadiusOutput.textContent = circleRadiusOutput + ' this is your radius output';
}
